/**
 * Single bump point for deploys — imported by sw.js via importScripts().
 * Increment SHIELDBELT_CACHE_VERSION on every release so old caches are dropped.
 */
var SHIELDBELT_CACHE_VERSION = '1.4.6';
